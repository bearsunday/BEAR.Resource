{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bear-resource-request.json",
  "title": "BEAR Resource Request",
  "description": "Schema for BEAR Resource request logging",
  "type": "object",
  "properties": {
    "uri": {
      "type": "string",
      "description": "The resource URI (e.g., app://self/user/profile)"
    },
    "method": {
      "type": "string", 
      "description": "The HTTP method (GET, POST, PUT, DELETE, etc.)"
    },
    "query": {
      "type": "object",
      "description": "The query parameters passed to the resource"
    }
  },
  "required": ["uri", "method", "query"]
}