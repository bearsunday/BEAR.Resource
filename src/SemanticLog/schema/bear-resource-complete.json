{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bear-resource-complete.json",
  "title": "BEAR Resource Complete",
  "description": "Schema for BEAR Resource completion logging",
  "type": "object",
  "properties": {
    "uri": {
      "type": "string",
      "description": "The resource URI (e.g., app://self/user/profile)"
    },
    "method": {
      "type": "string",
      "description": "The HTTP method (GET, POST, PUT, DELETE, etc.)"
    },
    "code": {
      "type": "integer",
      "description": "The HTTP status code of the response"
    },
    "headers": {
      "type": "object",
      "description": "The response headers"
    },
    "body": {
      "description": "The response body from the resource"
    },
    "view": {
      "type": ["string", "null"],
      "description": "The rendered view (may be null for some resources)"
    }
  },
  "required": ["uri", "method", "code", "headers", "body"]
}